import React, { FC } from "react";
import MainLayout from "../components/MainLayout";

const Payment: FC = () => {
  return (
    <MainLayout activePage="payment">
      <div>Payment</div>
    </MainLayout>
  );
};

export default Payment;
