import React, { FC } from "react";
import MainLayout from "../components/MainLayout";

const Maps: FC = () => {
  return (
    <MainLayout activePage="maps">
      <div>Maps</div>
    </MainLayout>
  );
};

export default Maps;
